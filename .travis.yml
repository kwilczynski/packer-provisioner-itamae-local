sudo: false

language: go
go:
  - 1.5
  - 1.6
  - tip

matrix:
  fast_finish: true
  allow_failures:
    - go: tip

branches:
  only:
    - master
    - develop

before_install:
  - make tools

install:
  - make deps

before_script:
  - make clean

script:
  - make test
